<!DOCTYPE html>
<html>
    <head>
        <title>javascript</title>
        <meta charset="utf-8">
        <link href="${staticcdnurl}/page/manage/css/main.css?${version}" rel="stylesheet">
    </head>
    
    
    <body>
        <script src="${staticcdnurl}/page/manage/js/main.js?${version}"></script>
        <div id="app" style="width: 100vw; background: red;">
            <span id="btn">点我</span>
          </div>
          <!-- <script >
                // 事件捕获：先输出 "外层click事件触发"; 再输出 "内层click事件触发"
                var useCapture = true;
                var btn = document.getElementById("btn");
                btn.addEventListener(
                  "click",
                  function(e) {
                    event.stopImmediatePropagation(); 
                    alert("内层click事件触发");
                  },
                  useCapture
                );
              
                var app = document.getElementById("app");
                app.onclick = function() {
                    alert("外层click事件触发");
                };
              </script> -->
    </body>
</html>